# Decisões tomadas na construção da skill sprintx

Ambiguidades do prompt de especificação resolvidas pela opção mais conservadora, uma linha de contexto cada.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DS-01 | O prompt não define onde a F5 registra o resultado da auditoria, mas a máquina de estados precisa detectar "auditoria aprovada" pelo disco | A F5 escreve `docs/sprintx/features/<slug>/00-AUDITORIA.md`; "aprovada" = o arquivo existe e contém a linha literal `VEREDITO: SIM` | Detecção pelo disco exige artefato; formato literal torna o portão binário |
| DS-02 | O prompt diz que ALTA volta para F3, mas não diz o que MÉDIA/BAIXA fazem no veredito | Veredito NÃO se e somente se existir achado ALTA; MÉDIA e BAIXA ficam registrados e não bloqueiam | É a única regra dura dada ("ALTA manda voltar"); bloquear por MÉDIA criaria regra nova não especificada |
| DS-03 | O prompt não define quando um PENDENTE é "bloqueante" | Todo PENDENTE é bloqueante por padrão; só vira não bloqueante com autorização explícita do usuário, com a premissa assumida registrada | Opção mais conservadora: na dúvida, trava a F3 em vez de planejar sobre suposição |
| DS-04 | Critério de detecção do modo EXTERNO vs INTERNO na F1, e o caso ambíguo | EXTERNO quando a feature nomeia ou depende de produto/API de terceiro; ambíguo = ingerir os dois lados; no EXTERNO ingerir também o mínimo interno tocado | Ingerir a mais é conservador; ingerir a menos gera plano cego |
| DS-05 | Quando criar `docs/sprintx/features/<slug>/` e `00-BLOQUEIOS.md` (a estrutura os lista, mas nenhuma fase os cria explicitamente) | O scaffold (pasta, `00-BLOQUEIOS.md` vazio, `base/00-INDICE.md`, `base/00-LACUNAS.md`) é o primeiro passo da F1 | F1 é a primeira fase que escreve em disco; ter `00-BLOQUEIOS.md` desde o início garante que ele exista quando a F6 precisar |
| DS-06 | Tensão entre "a skill executa a fase pendente em vez de obedecer fora de ordem" (PARTE 1) e "os commands devem recusar execução fora de ordem e apontar qual fase falta" (PARTE 2) | Os commands de fase recusam a fase pedida, apontam a que falta e executam a fase pendente | Concilia os dois trechos sem violar nenhum: recusa + aponta + segue o método |
| DS-07 | Reexecutar via command uma fase já concluída (ex.: `/sprintx-base` com base pronta) | Permitido só como complemento/revisão a pedido explícito, com aviso de que artefatos posteriores podem ficar desatualizados; caso contrário, executa a fase realmente pendente | Evita regeração destrutiva acidental de trabalho já auditado |
| DS-08 | Idioma dos arquivos da skill | Todo o conteúdo em pt-BR, inclusive a description do frontmatter | Prompt, marca e usuário são pt-BR; a description dispara igualmente bem no idioma do usuário |
| DS-09 | Como obter a data exigida no status das tasks na F6 | Sempre via `date +%Y-%m-%d` do sistema, nunca de memória | Data de memória do modelo pode estar errada; comando é verificável |
| DS-10 | O prompt manda a F6 rodar sem perguntas, mas não diz o que fazer quando não resta nenhuma task executável (tudo bloqueado) | Encerrar com o relatório final, com os bloqueios como pauta — nunca esperar resposta no meio da execução | "Nunca parar e esperar" é a regra dura; encerrar com relatório é a única saída compatível |
| DS-11 | Reauditoria (F5 rodada de novo após retorno à F3) | Sobrescreve `00-AUDITORIA.md`; histórico fica a cargo do versionamento do repositório | Um único arquivo mantém a máquina de estados simples e o portão inequívoco |
| DS-12 | A única pergunta permitida fora da F2 | Na F3, ao detectar algo que exigiria decisão humana em execução, a skill pergunta na hora e registra como nova linha D-NN — conforme o prompt ("transforma em decisão e pergunta na hora") | Está na especificação da F3; registrado aqui porque parece exceção à regra "só a F2 pergunta" |
| DS-13 | Marcadores de preenchimento nos templates | Padrão `{{assim}}` em todos os assets, com nota no topo mandando substituir tudo | Marcador inequívoco, fácil de grepar por vazamento |
| DS-14 | Ordem de execução com múltiplas sprints na F6 | Sprints em ordem numérica estrita; paralelismo só existe onde o plano declarou (entre fases/tasks) | O prompt só declara paralelismo em task e fase; entre sprints, sequencial é o conservador |
| DS-15 | A `description` do frontmatter é "texto voltado a humano" (grafia sprint^x) ou "frontmatter" (grafia sprintx)? | Frontmatter inteiro, inclusive descriptions, usa só a forma `sprintx`; `sprint^x` fica restrito ao corpo dos textos | O item 8 da verificação do prompt é explícito: em frontmatter só pode existir a forma sprintx |
| DS-16 | Achado de auditoria (verificação ponta a ponta): a regra 10 do SKILL.md ("F2 é a única fase que pergunta") contradizia a exceção da F3 (pergunta na hora para decisão nova) | Reescrita a regra 10 para declarar explicitamente a exceção da regra 11, e a regra 11 para citar a exceção — sem depender de DS-12 para reconciliar | Um agente seguindo só o SKILL.md (sem ler este arquivo de decisões durante a execução) não pode ficar em dúvida sobre uma pergunta legítima da F3 |
| DS-17 | Achado de auditoria: nenhum arquivo dizia onde `docs/sprintx/features/<slug>/` é ancorado em um monorepo | Adicionada seção "Onde fica docs/sprintx/features/<slug>/" no SKILL.md: raiz do repositório Git mais próxima; sem `.git` em nenhum ancestral, raiz do diretório de trabalho; reaproveitar `docs/` existente na raiz antes de criar um novo | Evita que o agente crie `docs/sprintx/features/<slug>/` dentro de um workspace individual de um monorepo por falta de instrução |

## Ajuste expx-schema v1 (frontmatter para o painel de operação)

Ambiguidades e conflitos do prompt de ajuste. Regra de ouro aplicada: onde o prompt conflita com a skill, **a skill vence** e o conflito é registrado aqui.

| # | Ambiguidade / conflito | Decisão tomada | Motivo |
|---|---|---|---|
| DS-18 | **Conflito prompt × skill:** o exemplo de `kind: tasks` no prompt traz `arquivos: [a.ts, b.ts]` (lista plana), mas o contrato de task do `SKILL.md` e o `TEMPLATE-tasks.md` já definem `arquivos` como mapa `cria:` / `altera:` | Mantida a forma da skill: `arquivos` é mapa com `cria` e `altera`, cada um uma lista | Regra de ouro (a skill vence) + item 5 da verificação, que exige os mesmos campos do contrato já definido; achatar a lista perderia a distinção cria/altera, que a F5 usa para detectar paralelismo falso |
| DS-19 | **Conflito prompt × skill:** o prompt usa ids de fase `F-01`, `F-03`; a skill já usa `F-NN.M` (`F-01.1`) em `TEMPLATE-fases.md`, `TEMPLATE-sprint.md` e no ORQUESTRADOR | Mantido `F-NN.M` em `fases:`, `fase:`, `paralela_com:` e `caminho_critico:` | Regra de ouro; trocar o id quebraria a correspondência entre YAML e prosa nos arquivos que a skill já gera |
| DS-20 | O prompt lista `estagio: f1..f6` e ids de fase `F-NN.M` sem dizer que são coisas diferentes; um parser poderia confundi-los | Documentada a distinção explicitamente no `00-schema.md`: `estagio` é a fase da máquina de estados; `F-NN.M` é o id de fase do plano; namespaces distintos | Duas grandezas com nome parecido no mesmo contrato é a origem clássica de bug de parser |
| DS-21 | O prompt define `status` de trabalho/sprint/fase (masculino) e de task (feminino), mas o `kind: decisoes` tem `status: fechada \| pendente`, que não está em nenhum dos dois enums | Tratado como enum próprio do kind `decisoes`, documentado como tal | É o único valor possível dado o exemplo do prompt; registrar evita que alguém "corrija" para `concluido` |
| DS-22 | A PARTE 1 exige frontmatter em `00-BLOQUEIOS.md` e `base/00-INDICE.md`, e a PARTE 4 exige template para todo arquivo listado — mas a skill não tinha template para nenhum dos dois (a F1 os criava inline) | Criados `assets/TEMPLATE-BLOQUEIOS.md` e `assets/TEMPLATE-base-indice.md`, referenciados na F1 e na tabela do `SKILL.md` | Sem template, a PARTE 4 ficaria descumprida para dois dos sete kinds; o conteúdo em prosa dos dois é exatamente o que a F1 já mandava escrever |
| DS-23 | O prompt não diz o valor inicial de `concluida_em` e `suite` para uma task recém-planejada na F3 | `concluida_em: null` e `suite: nao_executada` na F3 | Regra 6 do contrato (nunca omitir chave) + `nao_executada` é literalmente o estado de uma suíte que não rodou |
| DS-24 | O prompt não diz o que fazer quando a prosa de uma pasta antiga não permite inferir um valor na migração | Usar `null` (ou `[]`), nunca inventar, nunca perguntar, nunca parar; a chave sempre existe | Coerente com a regra 8 (dúvida vira bloqueio, não pergunta) e com a regra 6 do contrato; inventar valor entregaria dado falso ao painel |
| DS-25 | `00-AUDITORIA.md` não aparece na lista de kinds da PARTE 1 nem na lista explícita de "não recebem frontmatter" | Fica **sem** frontmatter, e isso está declarado no `00-schema.md` | A PARTE 1 é exaustiva quanto ao que o painel lê; inventar um `kind: auditoria` seria criar contrato não especificado. Registrado por ser omissão, não decisão explícita do prompt |
| DS-26 | Em `kind: fases`, o prompt traz `paralelizavel` e `paralela_com`, mas a prosa da skill só tem "Roda em paralelo com: nenhuma" | "nenhuma" na prosa ⇒ `paralela_com: []` **e** `paralelizavel: false`; com fase declarada ⇒ lista preenchida e `paralelizavel: true` | Mapeamento único possível entre a prosa existente e os dois campos novos, sem mudar a prosa |
| DS-27 | Travessões (`—`) apareceram dentro de marcadores `{{...}}` no frontmatter do `TEMPLATE-tasks.md` | Substituídos por hífen ASCII dentro do bloco de frontmatter | O item 7 da verificação pede frontmatter sem acento; manter o bloco 100% ASCII elimina o risco de um caractere não-ASCII vazar para o arquivo gerado |

## Ajuste de compatibilidade com o OpenCode (instalação dupla)

Fatos verificados no binário do OpenCode v1.18.23 instalado na máquina (tabela de locais embutida na própria documentação do binário), não presumidos.

| # | Ambiguidade / achado | Decisão tomada | Motivo |
|---|---|---|---|
| DS-28 | Onde o OpenCode procura skills, e se seria preciso manter duas cópias da skill | O OpenCode **auto-carrega** skills de `~/.claude/skills/<name>/SKILL.md` (as chama de *external skills*). Na instalação global a skill é instalada **uma só vez**, em `~/.claude/skills/sprintx/`, e serve aos dois harnesses | Verificado na tabela de locais embutida no binário. Duplicar a pasta criaria duas fontes divergindo com o tempo — o pior defeito possível para uma skill cujo contrato é o produto |
| DS-29 | Não existe ponte `.claude` → `.opencode` no escopo de projeto | Na instalação de projeto a skill é copiada de fato para `.opencode/skills/sprintx/`, além de `.claude/skills/sprintx/` | A auto-carga externa vale para `~/.claude/` (global), não para o `.claude/` do projeto; sem a cópia, o OpenCode não enxergaria a skill no projeto |
| DS-30 | Projetos vizinhos usam ora `command/` ora `commands/`; qual é o correto | Ambos funcionam (`.opencode/command(s)/`, `~/.config/opencode/command(s)/`); adotado o **singular** `command/`, que é a forma do exemplo do próprio binário | Verificado no binário. Registrado porque a divergência entre projetos sugeria erro onde não há |
| DS-31 | `argument-hint:` do frontmatter dos commands não existe no OpenCode | Os commands do OpenCode são gerados a partir dos do Claude Code removendo apenas essa linha; o corpo e o `$ARGUMENTS` são idênticos | `$ARGUMENTS` é suportado pelos dois (verificado no binário). Manter fonte única e remover um campo é mais seguro que manter sete arquivos escritos duas vezes à mão |
| DS-32 | Como espelhar a skill no repositório sem duplicar a fonte: symlink `.opencode/skills/sprintx` → `.claude/skills/sprintx`? | Descartado o symlink; o repositório versiona só `.claude/skills/sprintx` (fonte única) + `.opencode/command/`, e o instalador materializa as cópias | Symlink quebra em clone no Windows e em download por zip — frágil demais para o caminho de instalação, que precisa funcionar em qualquer máquina |
| DS-33 | O corpo da skill precisaria de variante por harness? | Não: verificado que a skill não cita `.claude` em lugar nenhum e só usa caminhos relativos à própria raiz, então os mesmos arquivos servem aos dois sem alteração | Confirma a promessa que o README já fazia ("pode ser movida sem quebrar nada"); nenhuma regra ou contrato do método precisou mudar por causa do OpenCode |
| DS-34 | O instalador poderia sobrescrever trabalho do usuário ao reinstalar | `install.sh` é idempotente (verificado: segunda execução produz árvore byte a byte idêntica), tem `--dry-run`, e só toca `skills/sprintx/` e os sete `sprintx*.md` — nunca outros arquivos do usuário | Instalador que apaga o que não é dele é defeito grave; o escopo estreito torna a reinstalação segura |

## Referência cruzada com a runx (metade Run do método Expx)

| # | Ambiguidade / achado | Decisão tomada | Motivo |
|---|---|---|---|
| DS-35 | Como referenciar a runx sem inventar o que ela é | Li o README e o `references/00-schema.md` da runx e espelhei a seção "Build e Run" com a mesma tabela comparativa e a mesma frase de fecho ("Muda o gatilho e o tamanho. Nunca o rigor.") | As duas metades precisam ler como um método só; texto espelhado é reconhecível de um repositório para o outro. A runx já referenciava a sprintx — a referência agora é recíproca |
| DS-36 | **Achado de verificação:** o README da runx afirma que os kinds compartilhados são "idênticos campo por campo", e eu tinha copiado essa afirmação | Comparei os dois schemas com parser: os 6 kinds batem no topo, mas a task da runx tem `teste_regressao` a mais. O texto foi corrigido para "os mesmos campos e enums, com a diferença de `teste_regressao`" | Publicar "idêntico campo a campo" seria uma afirmação falsa sobre o contrato — exatamente o tipo de imprecisão que o expx-schema existe para eliminar |
| DS-37 | `teste_regressao` deveria ser acrescentado também à task da sprintx, para igualar os contratos? | Não. A sprintx segue com os 10 campos do seu contrato + os 3 de execução | Uma feature nova não tem comportamento errado anterior a provar; o campo seria sempre nulo. Além disso, mudar o contrato da task violaria a regra de ouro do ajuste anterior (o contrato do SKILL.md vence) |
| DS-38 | Onde registrar que o schema é compartilhado, para não divergir com o tempo | Seção "Contrato irmão" no `references/00-schema.md`, com a regra: mudança em kind compartilhado vale para as duas skills; kind exclusivo evolui sozinho | O `00-schema.md` é o arquivo que alguém abre para alterar o contrato — o aviso tem que estar lá, não só no README |

## Ajuste da camada de estimativa (F3.5)

Ambiguidades e conflitos do prompt de ajuste da estimativa. Regra de ouro aplicada: onde o prompt conflita com a skill, **a skill vence** e o conflito é registrado aqui.

| # | Ambiguidade / conflito | Decisão tomada | Motivo |
|---|---|---|---|
| DS-39 | **Conflito prompt × skill:** o prompt cria uma "F3.5" entre F3 e F4, mas a regra inviolável 2 diz que "as fases são estritamente sequenciais; nunca pule fase" e a máquina de estados detecta a fase pelo disco — uma fase opcional não é detectável por disco sem quebrar a tabela | A regra 2 fica **intacta**. A F3.5 é declarada como a **única fase opcional** e a **única exceção** à sequencialidade, explicitamente fora da tabela de detecção: o disco continua indicando F4 com ou sem `00-ESTIMATIVA.md`. Ela é um desvio a partir da F3, disparado só por pedido do usuário ou pelo `/sprintx-estimar` | Regra de ouro: a skill vence, então nenhuma regra existente foi reescrita. Colocar a F3.5 na tabela de detecção faria a ausência da estimativa parecer uma fase pendente — exatamente o bloqueio que o prompt proíbe |
| DS-40 | O prompt exige "agregue de forma que o intervalo cresça menos que a soma linear" e "documente no reference exatamente o método", mas não escolhe o método | **PERT com variâncias somadas em quadratura**: `media=(o+4m+p)/6`, `desvio=(p-o)/6`, `desvio_conjunto=√(Σ desvio²)`. Gravado no frontmatter como `metodo_agregacao: pert_quadratura` | É o método clássico que satisfaz as duas exigências do prompt de uma vez: o PERT "favorece o provável" (peso 4 em `m`, literalmente o que o prompt pede) e a quadratura faz o intervalo crescer sublinearmente (√n em vez de n). E é aritmética simples, reproduzível à mão — o prompt exige auditável e reproduzível, o que descarta simulação de Monte Carlo |
| DS-41 | O prompt não diz o que acontece com a faixa agregada quando `media - desvio` fica absurdamente baixa (abaixo do melhor caso somado) | Piso duro: `min` nunca fica abaixo da soma dos otimistas do conjunto | Uma faixa agregada que promete menos que o melhor caso de cada task somado é aritmeticamente impossível de cumprir; publicá-la seria pior que não estimar |
| DS-42 | O prompt manda "buscar tasks comparáveis por tipo, área e sinais", mas a skill não tinha nenhuma taxonomia de tipo de task | Criado o enum `tipo_task` com 10 valores (`config`, `client`, `dominio`, `persistencia`, `api`, `ui`, `integracao_externa`, `teste`, `infra`, `refatoracao`), documentado no `00-schema.md` | Sem vocabulário fechado, "comparável por tipo" é irreproduzível: cada execução inventaria rótulos diferentes e o histórico nunca casaria. Os 10 valores cobrem os tipos que a própria sprint-01 do método já produz (config, client, teste) mais os do trabalho de negócio |
| DS-43 | O prompt manda aplicar fator de correção por "desvio sistemático", sem definir a partir de quantas amostras um desvio é sistemático | Fator só é aplicado com **3 ou mais** entradas encerradas daquele `tipo_task`; abaixo disso o desvio é declarado como ruído e nenhum fator é aplicado | Corrigir uma estimativa com base em uma única task anterior é ruído com aparência de calibração — o defeito exato que esta camada existe para evitar. 3 é o menor número em que uma média deixa de ser refém de um caso isolado |
| DS-44 | O prompt diz "confiança BAIXA se mais de um terço das tasks marcadas para quebrar", mas não diz o que a faixa total cobre nesse caso | Task mandada quebrar **não é estimada, não entra no esforço total nem no caminho crítico**, e a saída declara "N tasks fora da faixa". Se ela estiver na cadeia crítica, a saída diz que o caminho crítico está subestimado | O prompt é explícito: "sinaliza para quebrar a task, e não a estima". Incluí-la nos totais com um chute contradiria isso e contaminaria o número que se cobra |
| DS-45 | Onde vive `docs/sprintx/estimativas/HISTORICO.md`: dentro de `docs/sprintx/features/<slug>/` (como todo artefato da skill) ou fora? | **Fora**, em `docs/sprintx/estimativas/`, exatamente como o prompt o nomeia. É o único arquivo da skill que é do PROJETO e não de um trabalho, e o único que é **apendado**, nunca sobrescrito. Por isso seu `trabalho_id` de cabeçalho é `null` (a chave existe — regra 6 do contrato) e o `trabalho_id` real vive dentro de cada entrada | Calibração só funciona atravessando trabalhos; um histórico por feature nunca acumularia as 3 entradas necessárias para um fator. O `trabalho_id: null` foi a forma de honrar o cabeçalho comum obrigatório sem mentir sobre a que trabalho o arquivo pertence |
| DS-46 | O prompt lista os campos mínimos do `kind: estimativa` mas não diz como o painel distingue "sem calibração" de "calibração neutra" | `fator_correcao_aplicado: null` quando não houve calibração; **nunca `1.0`** | `1.0` é indistinguível de "apliquei um fator que por acaso deu neutro". `null` diz a verdade: não havia base |
| DS-47 | O prompt exige que a estimativa não bloqueie o fluxo, mas a F5 audita o trabalho de planejamento — auditaria a estimativa? | A F5 **não** audita nem exige `00-ESTIMATIVA.md`, e `references/05-auditoria.md` não foi tocado | Auditar a estimativa a tornaria de fato obrigatória, contrariando "opcional, nunca bloqueia". A F3.5 tem checklist de verificação própria, dentro do seu reference |
| DS-48 | Reexecutar a F3.5 sobre um plano já estimado, e sobre um plano já auditado | `00-ESTIMATIVA.md` é sobrescrito (histórico a cargo do versionamento, como em DS-11 para a auditoria); rodar a F3.5 sobre plano já auditado **não invalida a auditoria**, porque a F3.5 não altera nenhum arquivo do plano | Coerente com DS-11. A F3.5 é read-only sobre o plano por construção: se estimar revelar que uma task precisa quebrar, isso vai para `tasks_a_quebrar` e a correção acontece na F3, nunca aqui |
| DS-49 | O prompt manda a F6 registrar o esforço real, mas o contrato da task não tem campo de esforço, e mudá-lo violaria a regra de ouro | O real é registrado **na linha de status da prosa** de `tasks.md` (junto de data e resultado de suíte, onde a F6 já escreve) e consolidado em `HISTORICO.md`. **Nenhum campo novo no contrato da task nem no `kind: tasks`** | Regra de ouro: o contrato da task do `SKILL.md` não muda. O mesmo raciocínio de DS-37 (recusar `teste_regressao` na task da sprintx): o contrato da task é compartilhado com a runx e não se altera por conveniência de uma camada nova |
| DS-50 | Trabalho que rodou sem a F3.5: entra no histórico? | Sim, com `estimado_min`, `estimado_max`, `estimado_media` e `desvio` em `null` e o `real` preenchido | O real ainda alimenta a comparabilidade por tipo e área (dois dos três eixos de busca), mesmo sem servir ao cálculo de desvio. Descartá-lo jogaria fora dado de calibração que custou um trabalho inteiro para ser produzido |
| DS-51 | A unidade: o prompt diz "hora de trabalho focado" mas não diz se o frontmatter carrega a unidade no valor ou em campo próprio | Campo `unidade: h` próprio; os quatro campos de faixa são **números puros**, nunca strings com unidade embutida | String com unidade quebra qualquer soma ou comparação do lado do painel — o mesmo motivo pelo qual o expx-schema existe |
| DS-52 | O prompt pede o comando espelhado em `.opencode/commands/`, mas a skill usa `.opencode/command/` (singular, DS-30) | Gravado em `.opencode/command/sprintx-estimar.md`, singular, coerente com os outros sete | Regra de ouro: a convenção já estabelecida da skill vence. O conteúdo é idêntico menos `argument-hint:`, como manda DS-31 |
| DS-53 | O contador "7 arquivos" do `install.sh` ficaria errado com o oitavo comando | Trocado por contagem dinâmica (`$n`), verificado por `--dry-run`: reporta 8 nos dois harnesses | O instalador já copiava por glob `sprintx*.md`, então só o texto do log estava preso ao número. Contagem dinâmica não volta a errar no próximo comando |

## Ajuste da organização em disco (`docs/sprintx/`)

| # | Ambiguidade / conflito | Decisão tomada | Motivo |
|---|---|---|---|
| DS-54 | O pedido foi "uma pasta `sprintx` dentro de `docs`, e lá dentro uma pasta `features`", mas o `HISTORICO.md` é do projeto e não de uma feature — colocá-lo em `features/` seria mentira estrutural | `docs/sprintx/features/<slug>/` para as features e `docs/sprintx/estimativas/HISTORICO.md` como **irmão** de `features/`, não dentro dela | Honra o pedido (tudo sob `docs/sprintx/`, features em `features/`) sem quebrar DS-45, que estabeleceu que o histórico atravessa trabalhos. Um histórico dentro de uma feature nunca acumularia as 3 entradas que ativam um fator de correção |
| DS-55 | Pastas de features já criadas no formato antigo `docs/<slug>/` — migrar automaticamente? | **Não.** A skill trabalha nelas onde estão (a máquina de estados detecta a fase pelo conteúdo, não pelo caminho) e só cria features novas no formato novo. Mover é decisão do usuário | Coerente com a regra de migração de frontmatter já existente ("nunca reescreva em massa nem migre pastas que não vai tocar"). Mover pastas por conta própria quebraria links e o histórico do repositório do usuário |
| DS-56 | A seção "Onde fica" ancorava `docs/<slug>/` na raiz do Git; com o prefixo, o que exatamente é ancorado? | O ancorado passa a ser `docs/sprintx/`; a regra do monorepo continua idêntica, e a instrução de reaproveitar um `docs/` existente na raiz virou "crie `sprintx/` dentro dele" | Mantém intacta a regra de DS-17 (não criar dentro de workspace individual) apenas deslocando um nível o que é ancorado |

## Hooks, agentes e rastro (contrato expx-eventos v1)

Ambiguidades e achados na implementação de hooks e agentes. Regra de ouro aplicada: **o que foi verificado no harness vence o que está escrito no contrato** — e a divergência é registrada aqui e levada de volta ao contrato.

| # | Ambiguidade / achado | Decisão tomada | Motivo |
|---|---|---|---|
| DS-31 | **Conflito contrato × harness (verificado):** o `CONTRATO-expx-eventos.md` v1 afirma que no `PostToolUse` "exit 2 ali serve para o modelo ver o aviso". A documentação oficial do Claude Code diz o contrário: no `PostToolUse` o exit 2 NÃO bloqueia e o **stderr não volta ao modelo**; o único canal é JSON no stdout (`hookSpecificOutput.additionalContext`) | Os hooks de aviso emitem JSON no stdout, não exit 2 + stderr. O contrato foi corrigido no repositório Expx | Um hook de aviso que escreve em stderr no `PostToolUse` não avisa ninguém: falha silenciosa, o pior modo de falha para um mecanismo cujo propósito é justamente lembrar o modelo de uma regra |
| DS-32 | Onde hooks e agentes ficam, dado que o contrato assume um `plugin/` compartilhado pelas cinco skills e a sprintx é standalone | Cada harness na sua convenção: `.claude/hooks/`, `.claude/agents/`, `.claude/settings.json`; `.opencode/plugin/`, `.opencode/agent/`. A lógica mora uma vez só, em `.claude/hooks/*.sh`, e o plugin do OpenCode é uma ponte que os invoca | Duplicar a lógica criaria duas fontes divergindo com o tempo — o pior defeito possível para uma skill cujo contrato é o produto (mesmo raciocínio de DS-28) |
| DS-33 | **Lacuna de paridade (verificada no binário v1.18.23 e testada):** o `tool.execute.before` do OpenCode só sabe passar em silêncio ou lançar exceção. Não existe "permite mas avisa", que é o modo em que todo hook de método nasce | Bloqueio no OpenCode = lançar exceção em `tool.execute.before` (testado: a escrita não acontece e o modelo lê a mensagem). Aviso = anexar o texto ao resultado da ferramenta em `tool.execute.after`, prefixado com `[sprintx/hooks — aviso, a ação NÃO foi bloqueada]` | O prefixo é obrigatório: sem ele o modelo pode ler o aviso como saída da própria ferramenta. Testado ponta a ponta — o modelo entendeu que não foi bloqueado e repetiu a correção sugerida |
| DS-34 | O contrato manda declarar as ferramentas permitidas do agente, mas os dois harnesses usam campos diferentes | Claude Code: `tools: Read, Glob, Grep` (omitir o campo herdaria TODAS as ferramentas — seria o oposto do pretendido). OpenCode: `permission: { edit: deny, write: deny, bash: deny }`, pois não existe campo `tools:` no frontmatter dele | Verificado na documentação oficial e na tabela embutida no binário. Escrever de memória aqui produziria um agente de veredito com permissão de escrita, destruindo a garantia que justifica o agente |
| DS-35 | Como o hook descobre a task em andamento sem manter estado próprio (regra 6 do contrato) | Varre `docs/sprintx/features/*/sprint-*/tasks.md` procurando `status: em_andamento` no frontmatter. Sem task aberta, o hook não opina | Regra 6: toda decisão sai de arquivo que já existe. Medido em 76 ms por chamada, dentro do orçamento de 200 ms da regra 1 |
| DS-36 | O contrato define `regra_violada` (aviso) e `acao_bloqueada` (bloqueio) como eventos distintos | Um hook em modo bloqueio grava os DOIS: `regra_violada` (a regra foi violada) e `acao_bloqueada` (e a ação foi barrada) | São fatos diferentes e o painel usa cada um para uma coisa: violações decidem promoção a bloqueio; bloqueios mostram se algum hook está atrapalhando mais que ajudando |
| DS-37 | O contrato pede "duração real por task" do rastro para calibrar estimativa, e ao mesmo tempo alerta que tempo de parede não é esforço | O valor entra no `HISTORICO.md` como `duracao_observada`, campo distinto de `real`, e nunca o substitui; a calibração usa **mediana**, não média | Uma task "aberta" por seis horas pode ter tido vinte minutos de trabalho e um almoço no meio. Média é distorcida por um único intervalo com pausa; mediana quase não se move |
| DS-38 | `tdd-teste-antes` depende das convenções do `stackx`, que não existe ainda | Fica **inativo** (exit 0 imediato) quando não encontra `CONVENCOES.md`, em vez de adivinhar onde o teste deveria estar | Está na especificação ("sem `CONVENCOES.md`, fica inativo em vez de chutar"). Falso positivo desinstala o hook, e junto com ele vão os que funcionavam |

## Ajuste da camada de indexação (`FECHAMENTO.md` e campos de módulo/arquivos)

Ambiguidades e conflitos do prompt de ajuste que alimenta o `memox`. Regra de ouro aplicada: onde o prompt conflita com a skill, **a skill vence** e o conflito é registrado aqui.

| # | Ambiguidade / conflito | Decisão tomada | Motivo |
|---|---|---|---|
| DS-57 | **Conflito prompt × skill:** o prompt manda gravar o fechamento em `docs/<slug>/FECHAMENTO.md`, mas a skill organiza tudo sob `docs/sprintx/features/<slug>/` desde DS-54/DS-56 | O caminho é **`docs/sprintx/features/<slug>/FECHAMENTO.md`**. O `docs/<slug>/` do prompt é o formato antigo, que DS-55 mantém apenas para pastas que já existem | Regra de ouro: a skill vence. Gravar o fechamento fora da pasta da feature separaria o registro dos artefatos que ele resume, e reintroduziria em feature nova o layout que DS-54 substituiu. Em pasta de formato antigo, o fechamento nasce ao lado dos demais artefatos dela — a regra de DS-55 (trabalhar onde está) já cobre esse caso sem exceção nova |
| DS-58 | O prompt lista o passo de derivação para F3 e F6, mas quem grava o `ORQUESTRADOR.md` é a **F4** — sem tocá-la, os campos derivados na F3 não chegariam ao arquivo | `references/04-orquestrador.md` recebeu as linhas mínimas de wiring: grava o que a F3 derivou; se a F3 não deixou pronto, deriva pelo mesmo critério; `arquivos_alterados` nasce `[]` | O prompt descreve a intenção (derivar na F3, agregar na F6), não a topologia dos arquivos da skill. Deixar a F4 de fora produziria um campo derivado que nunca é gravado — a falha silenciosa que o expx-schema existe para evitar. Nenhuma regra da F4 mudou; só o preenchimento de um kind que ela já gravava |
| DS-59 | O prompt não diz se `arquivos_alterados` agrega tasks concluídas apenas, ou todas as do plano | **Só tasks `concluida`.** Task `bloqueada` ou `pendente` não entra | O campo responde "quem já mexeu neste arquivo": uma task bloqueada não mexeu em arquivo nenhum. Incluí-la faria o índice apontar para um arquivo que o trabalho nunca tocou — pior que a ausência, porque parece resposta |
| DS-60 | Um arquivo que uma task `cria` e outra `altera`: uma entrada ou duas? | **Uma.** A lista é de arquivos, não de eventos; a união é sobre o caminho | "Sem repetição" só tem uma leitura consistente com um índice invertido: a chave é o caminho do arquivo |
| DS-61 | O prompt manda derivar `modulo_afetado` da estrutura de pastas quando não há `CONVENCOES.md`, mas o hook `tdd-teste-antes` fica **inativo** nessa mesma situação (DS-38) — o critério deveria ser o mesmo? | **Não, e a divergência é deliberada e está registrada no reference:** o hook fica inativo, a derivação continua | São perguntas diferentes. O hook pergunta *onde o teste deveria estar*, que sem convenções é palpite e gera falso positivo (que desinstala o hook). A derivação pergunta *em que pasta o arquivo está*, que é fato observável. Ficar inativo aqui deixaria `modulo_afetado` vazio em todo projeto sem stackx — ou seja, quase todos |
| DS-62 | Que segmento do caminho é o módulo, dado que `src/`, `app/`, `packages/<nome>/src/` não são módulo | Primeiro segmento significativo após a raiz de código, ignorando invólucros declarados (`src`, `app`, `lib`, `packages/<nome>/src`, `internal`, `pkg`); arquivo na raiz vira `raiz` | Sem lista fechada de invólucros, cada execução escolheria um segmento diferente e o índice nunca casaria — o mesmo motivo que criou o enum `tipo_task` em DS-42. `raiz` evita o único caso em que a regra não teria resposta |
| DS-63 | `risco_residual` quando nada ficou por observar: `null` ou frase? | **Frase** (`Nenhum risco residual identificado`), nunca `null` | `null` é indistinguível de "não analisei". Um fechamento sem análise de risco é incompleto, não é um trabalho sem risco — e o índice precisa distinguir as duas coisas |
| DS-64 | O `kind: fechamento` leva `atualizado_em`, como todo arquivo de estado (regra universal 9)? | **Não.** Leva `fechado_em` e mais nada de data | Fechamento é registro de um instante, não estado que evolui. Um `atualizado_em` sugeriria que o arquivo é reescrito ao longo do tempo, que é justamente o que ele não é |
| DS-65 | O prompt diz que o `FECHAMENTO.md` "é o equivalente ao relatório técnico do runx" — deveria então ser o mesmo kind, para o índice ler os dois igual? | **Kinds distintos** (`fechamento` na sprintx, `relatorio_tecnico` na runx), com os **mesmos três campos de indexação** | Cumprem o mesmo papel no índice, mas com vocabulário de ciclos diferentes (uma feature não tem causa raiz; uma ocorrência não tem decisão principal de projeto). Unificar o kind forçaria campos sempre nulos de um lado — o mesmo raciocínio que recusou `teste_regressao` na task da sprintx (DS-37). O índice único é garantido pelos campos compartilhados, não pelo nome do kind |
| DS-66 | O `memox` não está instalado em lugar nenhum ainda; consultar uma ferramenta inexistente é inventar dependência? | A consulta é **condicional e silenciosa**: sem `memox`, a F1 e a F3 seguem sem registrar nada e sem avisar. A falta dele **não vira lacuna** em `00-LACUNAS.md` | O prompt é explícito ("ausência do memox nunca bloqueia"). Registrar a falta como lacuna a transformaria em pergunta obrigatória na F2 (regra da F1), que é exatamente o bloqueio proibido: lacuna é o que se procurou na fonte e não se achou, não uma ferramenta opcional que o projeto não instalou |
| DS-67 | O que o `memox` devolve é conhecimento externo — como conciliar com a regra 9 ("na F1 nada de invenção; todo número vem com a referência que o afirma")? | Entra na base como arquivo próprio, no template de recurso, com **proveniência preservada**: cada afirmação diz de qual `trabalho_id` e de qual artefato veio, e a seção "Fonte" aponta para o artefato, não para o `memox` | Assim a regra 9 é honrada sem exceção: é registro documentado de outro trabalho, não memória do modelo. Apontar a fonte para o `memox` esconderia a origem real e tornaria a afirmação inauditável |
| DS-68 | Trabalho que termina com tasks bloqueadas (nada mais executável): grava fechamento? | **Sim**, com o que foi entregue; os bloqueios abertos são o `risco_residual` | Se só trabalho perfeito fecha, o índice fica com viés de sobrevivência — e justamente o trabalho que travou é o que o próximo mais precisaria encontrar |


## Ajuste do estado da barra (`.expx/estado.json`, contrato `expx-estado` v1)

Ambiguidades e conflitos do prompt de ajuste que alimenta a barra de status do terminal. Regra de ouro aplicada: nenhuma regra ou comportamento existente muda; onde o prompt conflita com a skill, **a skill vence** e o conflito é registrado aqui.

| # | Ambiguidade / conflito | Decisão tomada | Motivo |
|---|---|---|---|
| DS-69 | **Conflito prompt × skill:** o prompt manda criar `references/08-estado.md`, mas esse número já é do `references/08-rastro.md` (contrato `expx-eventos`), referenciado por nome em quatro arquivos da skill | O arquivo é **`references/09-estado.md`** | Regra de ouro: a skill vence, e o prompt manda não alterar nada existente. Reusar o `08` sobrescreveria o rastro; renumerá-lo quebraria as referências que o `01`, o `05` e o `06` fazem a ele pelo nome. O prompt numerou pelo "próximo livre" sem conhecer a topologia atual — a intenção (um reference novo, transversal, ao lado do rastro) está preservada |
| DS-70 | O contrato canônico estava acessível no repositório do CLI e diverge do resumo do prompt em dois pontos: o exemplo traz um trabalho da **runx** (`OC-2026-0142`, `ferramenta: runx`, `fase: e3`, com `raio`/`branch` preenchidos), e o contrato traz três seções que o resumo não menciona (quem instala a barra, versionamento, ressalva sobre o OpenCode) | **O contrato canônico vence**, como o prompt determina. O `references/09-estado.md` traz as sete regras e a tabela de donos na íntegra; o exemplo foi trocado pelo da sprintx (`exportacao-csv`, `fase: f3`), e a ressalva sobre o OpenCode foi preservada | O prompt é explícito: contrato acessível vence o resumo. A divergência é de **exemplo**, não de contrato — o resumo do prompt já mostrava o mesmo objeto com valores da sprintx, o que é a ilustração certa para uma skill que nunca grava `e3` nem `raio`. **Nenhuma divergência de regra foi encontrada:** as sete regras do contrato e as do resumo são as mesmas |
| DS-71 | O prompt lista as fases que gravam (F3, F6, "demais que fazem transição") sem enumerá-las; quais references tocar? | **Todos os sete**: `01` (abertura + `fase: f2`), `02` (`f3`), `03` (`tasks_total` + `f4`/`f3.5`), `04` (`f5`), `05` (`f6`, e `f3` no retorno por veredito NÃO), `06` (`f6`, task, contadores, bloqueios, fecho), `07` (`f4` ao voltar da F3.5) | "Toda transição atualiza o estado" só é verdade se toda transição gravar. Deixar uma fase de fora produziria uma barra que trava num `f4` que já passou — pior que barra ausente, porque parece informação. Cada acréscimo é uma linha ao fim do passo que já existia; nenhum roteiro foi reescrito |
| DS-72 | Ao concluir o trabalho, o prompt manda `trabalho`, `fase` e `task` para `null` e não diz o que fazer com `tasks_concluidas`/`tasks_total` | **Ficam como estão** (ex.: `9/9`), não são zerados | O contrato só nomeia os três campos, e zerar o placar apagaria a única informação útil que sobra na barra logo depois do fim. `null` nesses três é o que diz "sem trabalho aberto" (regra 6 do contrato); o placar é histórico, não estado aberto |
| DS-73 | O que é o `titulo_curto` da sprintx, e o que fazer quando ele não existe? | A feature em linguagem de gente, minúscula e sem acento, **cortada em 30 caracteres**; o slug cru serve quando não houver nada melhor | O contrato manda cortar, não quebrar linha. O slug como fallback garante que o campo nunca fique `null` num trabalho aberto — e ele já é derivado do que o usuário disse, então é legível o bastante |
| DS-74 | `bloqueios` é a contagem total de bloqueios já registrados, ou só dos abertos? | Só os **abertos** (`resolvido_em: null`). Resolver um bloqueio diminui a contagem | O prompt manda gravar "ao registrar **ou resolver** bloqueio" — um contador que só sobe não teria por que ser reescrito ao resolver. Contagem acumulada faria a barra acusar bloqueio num trabalho já destravado |
| DS-75 | O prompt manda gravar `fase` a cada transição, mas há dois pontos em que o trabalho **não** avança: PENDENTE bloqueante travando a F3, e veredito NÃO voltando da F5 para a F3 | **A barra mostra onde o trabalho está, não onde deveria estar:** trava em F2 mantém `fase: f2`; retorno da F5 grava `fase: f3` | Um `estado.json` marcando `f3` num trabalho parado na F2 esconderia justamente o que o usuário precisa ver. É a mesma leitura do contrato ("somente exibição") aplicada ao caso de regressão de fase |
| DS-76 | Retomada de sessão interrompida: o `estado.json` pode ter morrido entre a gravação de `tasks.md` e a dele | A F6, ao carregar o mapa, **reconcilia** `tasks_total`, `tasks_concluidas` e `bloqueios` com o que o disco diz | É a consequência direta de "somente exibição": o disco é a verdade, o `estado.json` é a cópia. Reconciliar na retomada custa uma leitura que a F6 já faz de qualquer forma, e evita uma barra permanentemente defasada |
| DS-77 | Onde documentar a gravação atômica, já que o prompt proíbe elevá-la a regra inviolável? | No `references/09-estado.md`, como **procedimento** (temporário no mesmo diretório + `os.replace`), com a justificativa técnica | O prompt é explícito ("NÃO acrescente regra inviolável"): o arquivo é derivado e sua ausência é inofensiva. Procedimento no reference obriga na prática sem criar bloqueio por algo cosmético — a lista de 20 regras invioláveis segue **idêntica**, byte a byte |
| DS-78 | O contrato diz que `.expx/estado.json` é ignorado pelo versionador, mas nenhuma fase garantia isso — e neste repositório ele **não** estava ignorado | A F1 garante a linha `.expx/estado.json` no `.gitignore`, **na mesma passada** em que já garante `docs/eventos/` — e não faz nada se `.expx/` não existir | O contrato afirma o fato sem dizer quem o realiza; sem dono, "ignorado pelo versionador" vira intenção. A F1 já é a fase que mexe no `.gitignore` pelo mesmo motivo (arquivo local, reescrito o tempo todo, conflito de merge chato), então é acréscimo de uma linha num passo existente, não passo novo |
| DS-79 | `estado.json` corrompido ou com formato desconhecido: preservar o que dá, ou sobrescrever? | **Sobrescrever**, partindo do objeto completo com `null` no que não se sabe | Um arquivo derivado que não parseia não tem nada a preservar, e a barra já não o está lendo. É o único caso em que "nunca sobrescreva com um objeto novo" não se aplica — a regra existe para proteger campos de outros donos, e campos ilegíveis não são campos |
| DS-80 | Falha de gravação: registrar no rastro. E a ausência de `.expx/`? | Falha de gravação vira evento de rastro com `resultado: aviso`; **ausência de `.expx/` não registra nada** | O prompt separa os dois casos ("sem gravar, sem erro e sem aviso" para a ausência; "registre no rastro" para a falha). Não houve falha: houve ausência de ecossistema, e um aviso por trabalho num projeto sem CLI seria ruído permanente |

## Ajuste do diagrama do grafo de tasks (bloco Mermaid em `fases.md`)

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DS-81 | O prompt manda criar `references/09-diagrama.md`, mas o prefixo `09-` já é do `09-estado.md` | Mantido `09-diagrama.md`, como o prompt pede; o prefixo passa a ter dois arquivos | O nome foi dado explicitamente. O prefixo numérico dos references nunca foi chave única nem ordem de execução — é só agrupamento visual, e renumerar quebraria as referências cruzadas de todos os outros arquivos |
| DS-82 | `paralelizavel: true` com `depende_de` não vazio é contradição? A F5 chama de "paralelismo falso" o caso de tasks paralelas que **dependem uma da outra**, e o exemplo do `00-schema.md` traz `paralelizavel: true` sempre com `depende_de: []` | **É contradição e impede a geração**, como o prompt determina, e o `09-diagrama.md` diz explicitamente que é o achado 5 da F5 visto do lado do grafo | O prompt exige esse comportamento na verificação 5. Nenhuma regra existente foi alterada: a F5 continua com o mesmo achado, a mesma redação e a mesma severidade — o diagrama apenas se recusa a desenhar sobre dois campos que não podem ser ambos verdade, e manda corrigir no plano |
| DS-83 | O prompt não define "caminho crítico" para um grafo sem esforço por task | **Cadeia mais longa em número de tasks**, contada como 1 + o maior comprimento entre as dependências | Esforço não é campo do contrato da task (a F3 é proibida de escrever esforço no plano). Contar tasks é a única leitura derivável dos campos existentes, e é a que responde à pergunta "o que trava tudo" |
| DS-84 | Empate entre duas cadeias de mesmo comprimento | Vence a cadeia cujo último `id` é **menor** em ordem alfabética | Critério arbitrário, mas determinístico: o mesmo plano tem que gerar sempre o mesmo diagrama, senão o `fases.md` muda sozinho a cada regravação e polui o diff |
| DS-85 | "Título curto" — quanto é curto? | **28 caracteres**, cortando no espaço anterior ao limite, com reticências | O nó precisa caber ao lado de outros numa tela; acima disso o nó estica e o grafo vira escada. É a mesma ordem de grandeza do corte de 30 do `titulo_curto` (DS-73), sem ser o mesmo campo |
| DS-86 | Como marcar o caminho crítico sem apagar a cor de status | Classe `critico` só com `stroke-width`, aplicada **depois** das classes de status, numa linha `class` própria | Mermaid acumula classes: uma que só mexe na borda soma ao `fill` do status em vez de substituí-lo. Assim o nó mostra as duas informações ao mesmo tempo, que é o ponto do diagrama |
| DS-87 | Formato da linha `class`, já que a F6 precisa atualizar um nó só | **Uma linha `class` por task, um nó por linha**, na ordem dos ids; só a linha do `critico` agrupa nós | Torna a atualização da F6 uma troca de palavra numa linha, verificável por diff. Agrupar status por classe economizaria linhas e obrigaria a F6 a reescrever o bloco a cada task fechada |
| DS-88 | O prompt manda cortar acima de 25 tasks, mas não diz o que fazer com uma **fase** que sozinha passa de 25 | Gera o diagrama da fase mesmo assim e registra a observação de plano | O limite é da sprint, e abaixo de fase não existe subdivisão para onde quebrar. Não gerar deixaria a fase invisível, o que é pior; a observação faz o tamanho da fase aparecer como o problema de plano que é |
| DS-89 | Dependência que atravessa fases, no modo quebrado por fase | **Não aparece** no diagrama da fase; vive só no de visão geral | Nó fantasma de outra fase confundiria mais do que informa, e o diagrama de visão geral existe exatamente para carregar essa informação |
| DS-90 | `fases.md` de plano antigo, sem bloco Mermaid, ao fechar task na F6 | A F6 **não cria** o bloco: segue sem avisar | Gerar diagrama é papel da F3, sobre o plano inteiro. Uma F6 que criasse o bloco a partir de uma task só produziria diagrama parcial, e a regra de ouro é que nada existente muda |
| DS-91 | Onde documentar o diagrama sem elevá-lo a regra inviolável | Só em `references/09-diagrama.md`, com ponteiros a partir da F3, da F6, do template e da tabela do `SKILL.md` | O prompt é explícito ("NÃO acrescente regra inviolável"): o diagrama é derivado e sua ausência é inofensiva. A lista de 20 regras invioláveis segue **idêntica**, byte a byte — conferida por diff antes e depois |
| DS-92 | O item de diagrama na checklist do Passo 3 da F3 poderia virar portão de fase por tabela | O item existe, mas diz na própria linha que **não bloqueia** a fase | Sem a ressalva, um item de checklist vira critério de saída na prática, e o diagrama passaria a poder travar a F3 — exatamente o que a regra de ouro proíbe |

## Ajuste custo fixo (andaime condensado, suíte parcial, investigador por evidência)

Ajuste espelhado do que a runx recebeu, adaptado ao que difere entre as duas skills. Regra
de ouro: **nenhuma regra inviolável é acrescentada, removida ou renumerada — continuam 20**;
a regra 4 ganha uma frase sobre onde a suíte inteira é cobrada, e não muda de natureza.

**Origem.** Medição sobre 17 ocorrências fechadas de um projeto real (via runx) mostrou um
custo fixo de ~7.657 bytes por plano que não cai com o tamanho do trabalho — cabeçalhos de
frontmatter repetidos, três arquivos onde um bastaria. Em plano pequeno esse fixo é a maior
parte do custo.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DS-93 | O corte do andaime condensado na runx é "1 sprint e 1 fase no trabalho inteiro". Na sprintx isso quase nunca acontece: a regra 13 obriga a sprint-01 a ser capacidade de testar, então quase todo trabalho tem 2+ sprints | O corte é **por sprint**: qualquer sprint de **fase única** é gravada condensada, independente de quantas sprints o trabalho tem | Copiar o critério da runx ao pé da letra daria uma melhoria que nunca dispara. O que gera o custo é a sprint, não o trabalho — e o ganho por sprint de fase única é o mesmo nos dois casos |
| DS-94 | O arquivo condensado podia ter nome próprio (`PLANO.md`), mais claro para um arquivo que carrega sprint, fase e tasks | Continua sendo **`sprint-NN/tasks.md`** | O `task-so-fecha-verde.sh` casa `*/tasks.md` por caminho, e os hooks de método falham abertos: com nome novo, o portão deixaria de existir sem nada avisar. Vale para o legadox também, cujo `reversao-declarada.sh` casa `*tasks.md` |
| DS-95 | **Conflito com o contrato compartilhado.** `sprint`, `fases` e `tasks` são kinds compartilhados com a runx, e o `00-schema.md` proíbe mudá-los unilateralmente | Kind **novo**, `plano`, declarado igual nas duas skills; os três compartilhados permanecem intactos e continuam sendo o formato de sprint com duas ou mais fases | O `kind: plano` nasceu na runx e é compartilhado desde o início — as duas skills o gravam com os mesmos campos, e o painel o lê uma vez só |
| DS-96 | A runx cobra a suíte inteira no E4 (QA). **A sprintx não tem estágio equivalente**: a F5 audita o plano ANTES da execução, e a F6 termina sem portão de verificação independente | A suíte inteira é cobrada **ao fechar cada sprint**, no critério de saída da sprint — que já é um portão do método | Sem um lugar para a garantia ir, tirá-la de cada task seria perdê-la. O fim da sprint é o último ponto em que uma quebra colateral ainda é barata de achar. Escolha do usuário entre fim de sprint, fim de fase e fim do trabalho |
| DS-97 | Ao fechar a sprint com a suíte inteira verde, as tasks daquela sprint deveriam ter `suite` reescrito para `verde`? | **Não.** As tasks permanecem `parcial` | `suite` descreve o que rodou **para aquela task**. Reescrever em massa apagaria essa informação e faria o campo mentir sobre o que foi verificado quando |
| DS-98 | O agente `investigador` já dizia "use quando a base for grande", sem critério | Corte de **3 ou mais arquivos/páginas candidatos** na varredura inicial da F1; 1 ou 2, a sessão principal lê direto | "Grande" é juízo, e juízo a cada execução dá resposta diferente. A contagem sai de graça da varredura que a F1 já faz. `auditor-plano` e `revisor-testes` não têm corte: um é a verificação independente da F5, o outro responde uma pergunta só |
| DS-99 | O `task-so-fecha-verde.sh` exigia `suite: verde` para permitir `concluida` | Aceita `verde` **ou** `parcial`; continua barrando `vermelha` e `nao_executada` | Sem isso o hook barraria toda conclusão de task sob a regra nova. Verificado nos quatro valores em modo bloqueio, e verificado também que `parcial` sem os dois testes **continua barrando** — a regra 4 não afrouxou |
| DS-100 | O painel (`expxdev`) valida o frontmatter com Zod estrito e um mapa fechado de kinds | O painel foi atualizado **antes** desta mudança: `parcial` no enum, `kind: plano` no parser, e `montarSprints()` lendo os dois formatos | Zod **rejeita** valor fora do enum: sem o painel atualizado, um `tasks.md` com `parcial` sumiria do painel em vez de aparecer com defeito. Há agora um teste de contrato no repo do painel com as fixturas das duas skills |
| DS-101 | O que acontece com planos já escritos no formato de três arquivos | Nada: continuam válidos, não são migrados nem reescritos | Migração retroativa não traz economia — o custo já foi pago quando o plano foi escrito — e reescrever plano em andamento é risco puro |

## Ajuste sessões paralelas (worktree por feature, reivindicação de task, árvore limpa)

Ajuste espelhado do que a runx (skill irmã) recebeu, adaptado ao que difere entre as duas
skills: hooks em bash sem despachante único, ponte OpenCode já real (`sprintx.ts`), sem
MimoCode, sem suíte de testes prévia. Regra de ouro: nenhuma regra inviolável renumerada;
a lista ganha uma regra nova, a 21ª, e continua íntegra do DS-01 ao DS-101 acima.

**Origem.** O usuário relatou trabalhar com Claude Code, OpenCode e MimoCode ao mesmo tempo
no mesmo checkout, e pediu o mesmo mecanismo já entregue na runx (commit `32d04f3` daquele
repositório): `stash` de uma sessão levando trabalho de outra, suíte reprovando por código
que outra sessão está mudando, rastro de uma sessão caindo no arquivo da outra.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DS-102 | Como isolar features abertas ao mesmo tempo por sessões diferentes, sem mudar a heurística de detecção por `mtime` que `rastro_trabalho_id()` já usa | Nova **regra inviolável 21**: uma feature aberta por árvore de trabalho; com git, ela nasce em worktree próprio na F1 | Hook precisa de regra para apontar; é a mesma decisão já tomada e validada no runx (D-01 daquele plano) |
| DS-103 | Onde criar o worktree e com qual nome de branch, sem uma skill de entrega (mergex) acoplada da mesma forma que no runx | Diretório irmão `../<repo>--<slug>`, branch `feature/<slug>` | Aninhado duplicaria testes para runners; `feature/<slug>` é a convenção que a mergex já usa para trabalho vindo da sprintx — com ou sem mergex instalada, o nome nunca colide |
| DS-104 | Onde gravar o caminho do worktree, já que a sprintx não tem um kind por feature além do `orquestrador` | Chave `worktree` no kind `orquestrador` (compartilhado com o runx, mesmo nome e semântica) | Evita quebrar um painel que leia as duas skills; o `orquestrador` já é gravado uma vez, na F4, antes da execução |
| DS-105 | O que o worktree precisa herdar do checkout principal para os hooks funcionarem nele | Lista fechada: `.expx/hooks.json` + `estado.json` padrão, `.claude/settings.json`, `.env*` | Sem `.claude/settings.json`, os hooks nunca rodam no worktree — diferente do runx, aqui o registro dos hooks é o próprio `settings.json` do harness, não algo à parte que o instalador grava |
| DS-106 | `rastro_raiz` (bash) usava `[ -d "$d/.git" ]`, que falha num worktree (onde `.git` é arquivo com `gitdir: ...`) | `[ -e "$d/.git" ]` — aceita arquivo ou diretório | Provado em bancada: de um subdiretório do worktree, `[ -d ]` sozinho faz a busca subir demais e devolver a raiz errada. `sprintx.ts` (TypeScript) já usava `existsSync`, que não distingue tipo e por isso já funcionava — só o bash precisava do ajuste |
| DS-107 | Como uma sessão sabe que uma task já está sendo trabalhada por outra, sem lock que envelhece ou trava | Reivindicação pelo rastro: hook `task-reivindicada.sh` lê o `task_iniciada` mais recente daquela task e verifica se há `task_concluida`/`task_bloqueada` depois | `tasks.md` é kind compartilhado com o runx; um campo `executor` mudaria o contrato das duas skills. O rastro já registra as transições que a F6 precisa |
| DS-108 | Como identificar qual sessão gravou cada evento, sem cooperação garantida do harness | `sessao = <harness>@<id>`: `EXPX_SESSAO` → `CLAUDE_CODE_SESSION_ID` (com harness na frente) → ancestral de processo → `<harness>@sem-id` | Evidência já medida no runx, no mesmo ambiente onde a sprintx roda; funciona nos dois harnesses sem cooperação. `rastro_grava` já aceitava um parâmetro `extras` — nenhuma mudança de assinatura foi necessária |
| DS-109 | Onde checar árvore contaminada, já que a sprintx não tem uma fase de QA separada como o E4 do runx (a F5 audita o *plano*, antes da execução, nunca roda código) | Hook `arvore-limpa-antes-da-suite.sh`, no mesmo grupo `PreToolUse`/`Bash` de `git-perigoso.sh`; a checagem entra no critério de saída de sprint que já existe (DS-96), não numa fase nova | O portão real da sprintx é o fechamento de sprint. Uma árvore contaminada faz o portão não rodar a suíte e não fechar a sprint — sem registrar falha, porque fechar escondendo contaminação corrompe a mesma garantia que o portão existe para dar |
| DS-110 | Como validar os hooks novos sem a suíte de testes que a sprintx nunca teve (nenhum `test*.sh` existia antes desta mudança) | Script de bancada mínimo (`.claude/hooks/testes/bancada.sh`), não uma suíte completa equivalente às seis suítes do runx | Construir uma suíte completa é projeto próprio, maior que o escopo desta mudança. O script mínimo (13 casos) prova os dois hooks novos e a correção de `rastro.sh` sem virar a mudança principal |
| DS-111 | Se portar também o suporte a MimoCode, já que o runx acabou de ganhar essa capacidade | Não. MimoCode fica fora de escopo | A sprintx nunca teve suporte a MimoCode — não é regressão deixar de fora agora; adicionar exigiria criar do zero o que não existe (o `sprintx.ts` só cobre OpenCode), dobrando o escopo pedido |
| DS-112 | Como fazer `doctor.sh` listar os hooks novos, sem reescrever o formato do script | Acrescentar os dois nomes à string `HOOKS` já iterada por `for h in $HOOKS` | Mudança mínima; o script passa a listar oito hooks, mantendo a mesma tabela e o mesmo mecanismo de promoção de modo |
| DS-113 | Se "sem worktree" devia ser uma opção de configuração permanente em `.expx/hooks.json` | Não — é uma decisão pontual por feature, registrada na chave `worktree` do `ORQUESTRADOR.md` | Um interruptor global é o tipo de coisa que alguém esquece ligado; a decisão por feature é explícita e visível no arquivo-mapa daquele trabalho |
| DS-114 | `jq` sem a flag `-c` produzia pretty-print de várias linhas por objeto num pipeline de dois `jq` em sequência (`jq \| tail -1 \| jq`) em `task-reivindicada.sh`, quebrando o filtro silenciosamente | Corrigido com `jq -rc` no primeiro estágio do pipeline | Achado durante a bancada, não uma escolha de desenho: o `tail -1` pegava a última linha de um objeto JSON quebrado ao meio, e o segundo `jq` recebia fragmento inválido. Vale para qualquer hook bash futuro que precise filtrar o rastro por linha mais recente |
| DS-115 | `git status --porcelain` sem flag extra, usado no hook `arvore-limpa-antes-da-suite.sh`, para checar arquivo sujo fora do escopo | Adicionada a flag `--untracked-files=all` | Sem ela, um diretório inteiro não rastreado aparece como uma linha só (`?? src/`), não como os arquivos individuais dentro dele — o hook não conseguiria comparar contra a lista de escopo e deixaria passar sujeira que devia acusar. Mesmo achado já registrado no runx (D-83 daquele repositório) durante a mesma investigação de sessões paralelas |
| DS-116 | Verificar a sintaxe do `sprintx.ts` atualizado sem instalar `tsc` (não disponível na máquina, e Node 20.20.1 não tem `--experimental-strip-types`) | Checagem por balanceamento de chaves/parênteses/colchetes via script Python, sobre o arquivo com comentários e strings removidos | `node --check` não processa `.ts` nativamente. A checagem de balanceamento não substitui um compilador real, mas pega o tipo de erro que uma edição manual introduziria — registrado como limitação real, não como "sintaxe validada" sem ressalva |

## Ajuste densidade e forma de construção (as duas perguntas de abertura da F2)

Pedido do usuário no ecossistema irmão (prodx): toda avaliação completa que pode terminar em
trabalho deveria abrir com duas perguntas — quanta densidade de escopo o pedido merece
(MVP/Padrão/Completo/Profundo) e se o planejamento deve entrevistar o solicitante ou pesquisar
e decidir sozinho (Entrevista/Autônomo). Regra de ouro: nenhuma das 20 regras invioláveis foi
renumerada, removida ou teve sua natureza alterada — a regra 10 ganhou uma frase, como já
aconteceu antes (diagrama, sessões paralelas).

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DS-117 | O pedido original tratava "Entrevista vs Autônomo" como se pudesse pular a F2 inteira | **Não pula.** A F2 continua sendo a única fase do método que pergunta (regra 10). `autonomo` muda **como** os sete eixos são preenchidos — pesquisa e hipótese registrada em vez de bloco de 5 perguntas — nunca se a fase pergunta | Deixar o modo autônomo pular a F2 violaria a regra 10 e o princípio central do método: todo esforço vai para o planejamento. Um plano sem descoberta nenhuma reintroduziria exatamente a ambiguidade que a F2 existe para eliminar antes da F6 |
| DS-118 | Onde a densidade e o modo entram na máquina de fases, já que nem F1 nem F2 tinham esse conceito | **Passo 0 novo na F2**, antes do Passo 1 (preparar a entrevista). A F1 (Passo 1.1) só lê a sugestão vinda do `BRIEFING.md` do prodx, quando existir — não decide nada, porque a F1 não pergunta | A F1 não é o lugar de confirmar nada com o usuário (ela só ingere); a F2 é a única fase que pode confirmar. Ler cedo na F1 evita que a F2 refaça uma pergunta que o prodx já fez |
| DS-119 | Como registrar "autônomo assumiu isto" sem inventar um novo arquivo de estado, quando `00-DECISOES.md` já existe e é o kind compartilhado (`decisoes`) | Reaproveitar o campo `motivo` de cada `D-NN`: no modo autônomo, começa com `(HIPOTESE)` seguido da evidência. `D-00` é sempre a decisão de densidade/modo, e vira a primeira linha de toda F2 | Criar um kind novo só para "isto foi hipótese vs confirmado" duplicaria o que o campo `motivo` já carrega — motivo de uma decisão de pesquisa já é, por natureza, a evidência que a sustenta. Manter no mesmo kind evita que o painel precise aprender um contrato novo |
| DS-120 | Se `densidade` e `modo_construcao` no frontmatter do `kind: decisoes` quebram o painel que já lê esse kind com Zod estrito (ver DS-100, mesma preocupação já registrada para `kind: plano`) | Registrado como pendência de painel: os dois campos são **novos e opcionais na leitura antiga** (um painel desatualizado os ignora, porque Zod por padrão não falha em chave desconhecida a menos que `strict()` esteja declarado no objeto) — mas a extensão do schema Zod do painel para os dois campos fica marcada como trabalho a fazer antes de depender deles na UI | Diferente do DS-100 (onde um enum novo, `parcial`, seria REJEITADO por um enum fechado existente), aqui são chaves adicionais num objeto — o risco é assumido, não benigno por natureza. Sem o painel atualizado, os dois campos existem no arquivo mas ficam invisíveis nele, o que é seguro, mas incompleto |
| DS-121 | Se o modo autônomo dispensa a pergunta dos eixos 1 (escopo de negócio) e 7 (definição de pronto), que são notoriamente os mais dependentes de quem pediu | **Não dispensa.** São citados explicitamente em `02-descoberta.md` como os dois eixos que tendem a continuar como pergunta direta mesmo em modo autônomo, porque nenhuma leitura de código responde "o cliente ficaria satisfeito com isso" | Um modo autônomo que decidisse escopo de negócio sozinho pararia de ser planejamento e viraria suposição — exatamente o que a regra 8 (dúvida vira pergunta, nunca suposição) e o princípio central proíbem |
| DS-122 | Se a densidade (MVP/Padrão/Completo/Profundo) deveria cortar quais dos sete eixos são perguntados | **Não corta eixo nenhum.** Densidade muda a PROFUNDIDADE de cada eixo (quanto se investiga casos de borda e consequências de segunda ordem), nunca a lista de eixos obrigatórios | Os sete eixos cobrem riscos estruturalmente diferentes (dado, erro, ambiente, observabilidade); pular um por causa de "é só um MVP" é a receita clássica de MVP que não tem onde ligar log quando quebra em produção. "MVP" nesta skill significa menos escopo de negócio, não menos rigor de plano |

## Ownership de estado local, do HISTORICO.md e dos dois formatos de plano (E2E BuildX → SprintX → MergeX)

Achados de um E2E real das três skills. O E2E parou no portão E2/V9 da mergex, que acusou
`.gitignore` como desvio fora do plano — corretamente, porque a F1 o alterava para proteger
arquivos que a própria skill descreve como estado **local da máquina**. No mesmo trabalho
apareceram outras duas incoerências internas: `HISTORICO.md` ficava não rastreado ao fim da F6,
e o formato condensado de sprint, que a F3 já sabia gravar, não era aceito pelos leitores de
F4, F5 e F6. As decisões abaixo são **append-only**: nenhuma decisão anterior foi renumerada
nem reescrita.

| # | Ambiguidade | Decisão tomada | Motivo |
|---|---|---|---|
| DS-123 | Como manter `docs/eventos/` e `.expx/estado.json` fora do versionador sem que a F1 altere o `.gitignore` versionado — **supera a DS-78**, que dava esse ownership à F1 | A F1 usa o mecanismo **local** do repositório: `git check-ignore` primeiro (já ignorado = no-op) e, só se necessário, uma linha idempotente no arquivo devolvido por `git rev-parse --git-path info/exclude`. A skill **nunca** modifica `.gitignore` | Estado da máquina não pode virar mudança da feature. A DS-78 resolvia o sintoma ("ninguém garantia o ignore") criando um problema maior: uma alteração global, versionada, feita na F1 — antes de o plano existir — que suja a branch e aparece como desvio legítimo no portão de prontidão da mergex. O `info/exclude` dá a mesma garantia sem entrar em `git status`. Um time que queira política compartilhada versiona as entradas por fora da F1, e o passo do `check-ignore` já as respeita |
| DS-124 | Por que resolver o caminho do exclude com `git rev-parse --git-path info/exclude` em vez de escrever `.git/info/exclude` | Sempre pelo comando | Num linked worktree — que é como a regra 21 abre toda feature — `.git` é um **arquivo**, não um diretório, e o exclude real vive no repositório principal. O caminho fixo gravaria no lugar errado ou em lugar nenhum, e o modo de falhar é silencioso: os arquivos simplesmente voltariam a aparecer sujos, exatamente no worktree onde a F6 roda |
